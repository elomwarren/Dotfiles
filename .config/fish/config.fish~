if status is-interactive
    # Commands to run in interactive sessions can go here
atuin init fish | source
end

# Install Starship
starship init fish | source

# Alias to mimic bash alias "update"
alias update "bash -c 'update'"
